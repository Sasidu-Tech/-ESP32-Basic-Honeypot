# -ESP32-Basic-Honeypot
A simple cybersecurity project built with an ESP32 to demonstrate the basic concept of a Honeypot.  The ESP32 runs a fake HTTP web service that can detect and log incoming web requests. When a client accesses the honeypot, the system records the client's IP address, request information, and request count through the Serial Monitor.
